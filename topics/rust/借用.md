---
title: Rust 借用
type: concept
tags: [rust, programming, memory, borrow]
difficulty: 3
---

# Rust 借用

借用在 [[Rust 所有权]] 的基础上，让你可以**引用**值而不获取所有权。

## 不可变借用

```rust
fn main() {
    let s = String::from("hello");
    let len = calculate_length(&s);  // &s 创建一个引用
    println!("'{}' 的长度是 {}", s, len);  // s 仍然有效
}

fn calculate_length(s: &String) -> usize {
    s.len()
}
```

## 可变借用

```rust
fn main() {
    let mut s = String::from("hello");
    change(&mut s);
    println!("{}", s);  // "hello, world"
}

fn change(s: &mut String) {
    s.push_str(", world");
}
```

## 借用规则

1. 同一时刻，**一个值可以有多个不可变借用**（`&T`）
2. 同一时刻，**只能有一个可变借用**（`&mut T`）
3. **可变和不可变借用不能同时存在**

## 关联

- [[Rust 所有权]]：基础
- [[Rust 生命周期]]：借用何时合法
